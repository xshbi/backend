# Backend Server Configuration
PORT=3000

# CORS Configuration
# Allowed origins for CORS requests (comma-separated or default to localhost:5173)
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Database Configuration
# (Add your existing DB config here)

# JWT Configuration
# (Add your existing JWT config here)
